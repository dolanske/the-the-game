### TO-DO


- When editing a level, create a copy of current level and save any changes to it, while also still using the object array as stated lower.
    [] Add check, if object already exists at given position in 'generateObjectAtPosition'

[] Create a new file called create js, store all variable declarations there

[] Rework solidObjects Array. Should be an object of arrays split into 
    - solid objects
    - non-solid objects
    - liquids
    - entities (player, items, enemies)